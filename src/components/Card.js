import React, { Component } from 'react';

export default class Card extends Component {
    state = {
        characters: []
    }

    render() {
        return(
            <div className="card">
                
            </div>
        );
    }
}